# Chronos dev deploy of slice 1 (2026-09-10)

The `fix/chronos-review-decisions-2026-09-09` branch (28 commits: slice 1 of the
`api.js` retirement, the hygiene and kernel-hardening commits, the Phoenix pin)
landed on `dev` and was deployed to Chronos dev. Every step below was read back
from the cluster or from CI, not assumed.

## Sequence

| Step                      | Result                                                                                                                                                                                                                                                          |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local CI-equivalent gates | typecheck, lint, secrets test, `deps:check`, dataset validation, unit (4696), integration (489) all green on the branch                                                                                                                                         |
| Deploy-surface diff       | `k8s/base` and `k8s/overlays/dev` render identically at `dev` and at `phoenix-last-full`, so the pin changes nothing live; the chronos-dev ConfigMap keys match the schema (only `PROD_DATABASE_URL` is required, from the secret)                              |
| X/F3 Phoenix pin          | tag `phoenix-last-full` (`36b4e998`) pushed; `argocd/applications/dev.yaml` pinned in `7788b0e5`; the live Phoenix app `hack-for-facts-eb-server-dev` patched to `targetRevision: phoenix-last-full`, `automated` removed; still Synced / Healthy at `c22d10d4` |
| `dev` fast-forward        | `c22d10d4..7788b0e5`, 28 commits, owner-approved                                                                                                                                                                                                                |
| Dev pipeline              | run 34494731400 green (type check, lint, validate, unit, integration, build, Trivy); pushed `0b0dbb7a ci(image): Pin Chronos dev 7788b0e581ec`                                                                                                                  |
| Chronos sync              | `transparenta-eu-dev-server` synced by hand to `0b0dbb7a` (ConfigMap + Deployment OutOfSync, everything else unchanged; secrets apps Synced / Healthy before and after); rollout 2/2, 0 restarts, image `7788b0e5…`                                             |
| Probes                    | `/api/v1/live` 200, `/api/v1/health` healthy (postgres, meilisearch, opensearch, user database; synthetic disabled), `/api/v1/ready` 200, `/api/v1/graphql` answers, `/api/v1/mcp` 405 on GET as designed                                                       |
| Cutover replay, pre-sync  | image `01ccc64b`: 63 cases, 10 pass, 5 pass with warnings, 48 fail, 2 stale allowlist entries — the review's §5.1 result exactly                                                                                                                                |
| Cutover replay, post-sync | image `7788b0e5`: the same 48 failing cases (set diff empty), the same difference counts (contract breaks 3,972; data parity 70,764; rounding 0); no regression from the new image                                                                              |

Replay reports: `/tmp/gm-pre/2026-09-10T15-19-08-134Z-9509eb6f` and
`/tmp/gm-post/2026-09-10T15-24-57-783Z-cbe6e848` on the lead's machine (not
committed; regenerate with `pnpm test:gm:cutover` against the two dev hosts).

## Other checks on the new image

| Check                                  | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MCP (`/api/v1/mcp`)                    | `initialize` 200 (protocol 2025-06-18, tools + resources), `tools/list` 200 with 72 tools; GET 405 as designed                                                                                                                                                                                                                                                                                                                                                                |
| REST status smoke                      | `/advanced-map-datasets/public` 200, `/advanced-map-analytics/public/<unknown>` 404, `/pnrr/release` 200, `/short-links/<unknown>` 404; `/pnrr/overview` answers 503 `PNRR_UNAVAILABLE` on the NEW image and on the frozen Phoenix dev alike (pre-existing)                                                                                                                                                                                                                   |
| Snapshot golden suite (`pnpm test:gm`) | 9 pass, 5 fail against Chronos dev: the two per-capita cases the spec documents as failing by design, plus `monthly-totals`, `filtered-by-entity-type`, `monthly-income-vs-expenses`. The SAME five fail against the frozen Phoenix dev kernel (image `b80c4778`), so they pre-date slice 1; the last three are snapshot-vs-data deltas (revised 2023 monthly values; Phoenix `admin_*` entity types vs Chronos `uat`, review DP-04) and need re-baselining, not a server fix |

## What the replay says, and what it does not

"No regression" above means: on the legacy client corpus over `/api/v1/graphql`,
the new image answers exactly as the previous Chronos image did. The corpus
does not exercise the native-aware documents the Chronos dev client sends, nor
the REST or MCP surfaces beyond the smokes above; those remain covered by the
unit and integration suites CI ran, not by a live replay.

The 48 failures are the ones §5.1 classifies: roots not on the kernel by
decision (B/F1, client migration is the gate), the native INS dimension-slug
contract, the undeclared INS value-level deltas awaiting an allowlist decision,
and data-vintage drift. Two allowlist entries matched no difference any more;
WP1 below re-pinned them.

## Later syncs

| Date       | Commit / image                                | What changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Verified                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-10 | `83d1747c` (pin `7af594f1`), synced 19:16 UTC | Codebase plan WP1 (`codebase-plan-2026-09-10.md`): five kernel-replay snapshots re-baselined with verified causes (exact-year national POP107D denominator, the waived Phoenix 2023-02/03 monthly defect, the `uat` entity type), the two stale allowlist pins moved to the 2026-09-05 catalog counts (760 / 1,121), doc 13 §7 rows 1–2 corrected. No runtime code changed; the image differs from `7788b0e5` only by test assets and docs.                                                                                                                      | 2/2 pods, 0 restarts, Argo Synced/Healthy; `/api/v1/live`, `/health`, `/ready` 200 with every dependency ok; cutover replay against the deployed host: **46** failing (the two picker classification cases now pass-with-warnings under the corrected pins), 0 stale entries, contract-break 3,972 / data-parity 70,764 unchanged, id set identical to the local run; snapshot suite 14/14 against the deployed host. |
| 2026-09-11 | `56324afe` (pin `161cf0ae`), synced 08:05 UTC | Codebase plan WP2: the corpus as a test asset. Runtime code touched only by an optional test-only `legacyDimensions` injection on the budget module, two exported root-name constants and the exported resolver-merge helper; the rest is tests (`kernel-legacy-roots`, `redesign-composition`, the harness partition of failing cases by kernel-mounted roots) and the INS fake-repository move into `tests/fixtures/`.                                                                                                                                         | 2/2 pods, 0 restarts, Argo Synced/Healthy; `/api/v1/live`, `/health`, `/ready` 200 with every dependency ok; cutover replay against the deployed host: 46 failing (id set identical to the WP1 run), 25 of them on kernel-mounted roots without a recorded parity decision (the new partition), 0 stale entries, contract-break 3,972 / data-parity 70,764 unchanged; snapshot suite 14/14 against the deployed host. |
| 2026-09-11 | `e40a60b8` (pin `455c3810`), synced 09:42 UTC | Codebase plan WP3: tests only, no runtime code. The native INS repo's SQL and snapshot policy pinned over the capturing driver (which now records transaction settings), the population readers and native factors (T-13), the owner write fence (T-06), the real jose-signed request-level authentication on `buildRedesignApp`, and a shared Testcontainers helper that lets `map-owner-deletion` and `normalization-factor-set` run without a hand-prepared database (verified 14/14 on the zeus Docker host; e2e stays outside the dev-branch CI).           | 2/2 pods, 0 restarts, Argo Synced/Healthy; `/api/v1/live`, `/health`, `/ready` 200 with every dependency ok; cutover replay against the deployed host: 46 failing, id set identical to the WP1 and WP2 runs, 25 on kernel-mounted roots without a recorded parity decision, 0 stale entries, contract-break 3,972 / data-parity 70,764 unchanged; snapshot suite 14/14 against the deployed host.                     |
| 2026-09-11 | `940047e1` (pin `0d8cf542`), synced 10:22 UTC | Codebase plan WP4: types only, no query rewritten (the 21 SQL pin suites compile the same text). DP-07's last four tables typed with `privacy_class` and `core.organizations`' column made non-null; Bucharest named once in `shared/core/territory-constants.ts` with the budget and INS modules re-exporting and the raw SQL reading it through `sql.lit`. The ins-native e2e suite runs on any Docker host. First sync attempt applied against a stale manifest cache (pods stayed on `e40a60b8`); a hard refresh and a second sync rolled the new image out. | 2/2 pods, 0 restarts, Argo Synced/Healthy; `/api/v1/live`, `/health`, `/ready` 200 with every dependency ok; cutover replay against the deployed host: 46 failing, id set identical to the WP1–WP3 runs, 25 on kernel-mounted roots without a recorded parity decision, 0 stale entries, contract-break 3,972 / data-parity 70,764 unchanged; snapshot suite 14/14 against the deployed host.                         |
| 2026-09-11 | `3d2dd2c6` (pin `83b2f5e0`), synced 11:29 UTC | Codebase plan WP5: pure moves only. `budget-repo.ts` split into five read families behind the unchanged `makeBudgetRepo` (2,436 → 168 lines) and `ins-repo.ts` into three behind the unchanged factory (958 → 62), one reviewed commit per family with zero body-line residue on every one; plus the budget legacy e2e suite made runnable on any Docker host (266/266 with the INS suite on zeus). Synced with the hard refresh first; the rollout landed the new image on the first attempt.                                                                   | 2/2 pods, 0 restarts, Argo Synced/Healthy; `/api/v1/live`, `/health`, `/ready` 200 with every dependency ok; cutover replay against the deployed host: 46 failing, id set identical to the WP1–WP4 runs, 25 on kernel-mounted roots without a recorded parity decision, 0 stale entries, contract-break 3,972 / data-parity 70,764 unchanged; snapshot suite 14/14 against the deployed host.                         |

## Standing state

- Phoenix dev is frozen at `phoenix-last-full` and no longer follows `dev`; it
  is the cutover baseline. Unpin only with a production plan.
- Chronos dev tracks `dev` with manual sync; every push to `dev` builds an image
  and lands a pin commit, and nothing deploys until the app is synced. The
  commit recording this deploy itself lands a docs-only pin, so the Argo app
  shows the Deployment OutOfSync against a code-identical image until someone
  chooses to sync it; the deployed and verified image is `3d2dd2c6` (WP5,
  see "Later syncs").
- Production (`main`) untouched, per the k8s README boundary.
