# Chronos migration review — consolidated findings (2026-09-09)

Tree reviewed: `dev` @ `85ecf30e` (working tree clean). Window: every commit since
`b4e0ec38` "Prepare Chronos redesign API" (2026-08-24), ~250 commits, 446 files.
Live facts are dated 2026-09-09 unless stated: Chronos `transparenta_prod`
(INS publication run 22915, factor set 2 run 23230), Phoenix dev `hack-for-facts-dev`
(budget import 2026-01-25, INS sync 2026-02-05), the two dev deployments, and CI.

Method: seven parallel read-only review agents (one per area, files below), each
writing a findings table with `confirmed` / `unverified` status and file:line or SQL
evidence. The lead then independently re-ran the highest-impact checks (SQL on both
databases, endpoint probes, k8s reads, CI job conclusions). No source, test, config,
or manifest file was changed; the only new files are in this directory. Nothing is
committed.

Per-area reports (read these for evidence and full tables):

| Area                                                       | File                                                                   | Findings                           |
| ---------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------- |
| Budget legacy roots on the kernel                          | [budget-legacy-roots.md](./budget-legacy-roots.md)                     | F1–F23                             |
| INS native module + INS data parity                        | [ins.md](./ins.md)                                                     | INS-01–INS-17                      |
| Budget data parity, schema usage, read-only guarantee      | [data-parity.md](./data-parity.md)                                     | DP-01–DP-15                        |
| Composition, infra, deployment, CI mechanics               | [infra-composition-deploy.md](./infra-composition-deploy.md)           | F1–F27 (infra)                     |
| Normalization factors, population, maps, reference, kernel | [normalization-population-maps.md](./normalization-population-maps.md) | N1–N7, M1–M4, R1–R3, K1, P1–P5, T1 |
| Tests and CI health                                        | [tests-ci.md](./tests-ci.md)                                           | T-01–T-23                          |
| Other source modules, interfaces, MCP, auth                | [modules-interfaces.md](./modules-interfaces.md)                       | see file                           |

IDs below are `<file-prefix>/<id>`: `B/` budget, `I/` ins, `D/` data-parity,
`X/` infra, `N/` normalization, `T/` tests, `M/` modules.

## 1. Headline verdicts

1. **Chronos dev is Phoenix-free and matches its pin.** The pod runs
   `dist/redesign-api.js`; its secret carries only the serving-DB URL and Clerk keys;
   egress is default-deny to Chronos Postgres, Meili, OpenSearch and the dev user DB;
   the live image equals the overlay pin at HEAD and Argo is synced to `85ecf30e`.
   Every money path on that deployment uses one factor source, promoted set 2
   (digest matches the code pin). Verified by three agents and the lead.
2. **The hazard runs the other way: Phoenix dev embeds a stale kernel.** Phoenix dev
   (server image `b80c4778`, 2026-09-04, auto-synced manifests) serves
   `/api/v1/graphql` from `build-app.ts` with `REDESIGN_SURFACE_ENABLED=true`. On that
   surface `executionAnalytics` uses unpromoted factor set 1 with carry-forward,
   `budgetEntityRanking`/heatmaps/timeseries use an embedded JS-float table (2025 EUR
   5.05, GDP 1.88 T, 2026 estimates), and `entityAnalytics` does not exist yet
   (lead probe 2026-09-09). Prod exposes no kernel surface (`/api/v1/graphql` → 404),
   so nothing here is prod-facing today. **No P0.**
3. **One P1 money defect, upstream, server-visible (D/DP-01).** Chronos flags
   `is_yearly` per (entity, year, report type, main creditor), so an entity whose main
   creditor changed mid-year has two yearly rows and the annual MV sums both. Lead
   re-verified: entity 26429279 2024 detailed expense is 2.92 bn on Chronos vs
   2.05 bn on Phoenix; exposure is the disjoint-handover class only: 115 / 1 / 54
   scopes in 2023 / 2024 / 2025 (the other ~6,150 multi-creditor scopes per year are
   parallel reporting and sum correctly on both sides). The fix belongs to the
   scrapper fact rebuild (T104000 `is_year_end` flags are still unpopulated); the
   server cannot correct it from the annual MV.
4. **Scope gap vs doc 13.** `heatmapCountyData`, `heatmapUATData`,
   `commitmentsSummary/Aggregated/Analytics`, `entities`, `uats`, `datasets`,
   `staticChartAnalytics` are not on the kernel although doc 13 §2/§4 assign them to
   kernel modules (B/F1, T/T-09, N §Q5; lead probe confirms). 21 of 63 cutover cases
   are red by construction until this is ported or the doc is amended to "replaced by
   native roots + client migration".
5. **CI has been lying green.** Two different harnesses are involved. The nightly
   _snapshot_ golden master (12 hand-written specs against one legacy `/graphql`
   URL) targets a dev hostname with no ingress (`dev-api.` instead of `api-dev.`),
   so its Dev job has returned 404 on every run since 2026-03-07; its Prod job
   compares Phoenix prod against snapshots recorded before an entity-name casing
   change and reports 38 stale mismatches. Both jobs are `continue-on-error`, so
   the workflow shows success either way (T/T-01..03; lead verified the last run's
   job conclusions). Prod itself is not implicated: it has no kernel surface. The
   _cutover replay_ (63 client documents, legacy vs kernel) runs nowhere in CI,
   `tests/e2e` (51 files, the only real-Postgres proofs) never runs in CI, and
   CodeQL scans `main` only.
6. **Code quality of the new slices is high on the rules that matter.** Across all
   seven areas: no `throw` in `core/`, no float on money on the Chronos path,
   no raw `JSON.parse`, no write SQL against the serving DB, no cross-source
   internal imports, SDL byte-identity tests for the carried roots, and Decimal end
   to end. Privacy: the kernel identity hub, search, parliament votes, INS and the
   grouped budget roots gate `privacy_class` in SQL; five reads in legal, companies
   and the kernel territory repo do not (§2.1b), and the lead confirmed every one
   of those tables holds zero non-public rows today, so they are latent gaps, not
   exposures. The gates pass on this tree (typecheck, lint 0 warnings,
   deps:check; 5,719 unit/integration tests pass, one known load flake).

## 2. Consolidated findings

Severity: P1 fix before prod cutover · P2 fix during the migration · P3 hygiene.
Status: every row is `confirmed` unless marked.

### 2.1 Server bugs (code)

| ID          | Sev                         | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Where                                                                                                                                           |
| ----------- | --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| X/F1        | P1                          | Liveness/startup probes time out at 3 s while the kernel health probe bounds each dependency at 4 s and pool acquisition at 10 s; a hung Meili/OpenSearch or a saturated 10-conn pool restarts a healthy pod. Lead verified the 3 s / 4 s values.                                                                                                                                                                                                                                                                                                     | `k8s/base/deployment.yaml:95-109`, `src/modules/shared/index.ts:221`                                                                            |
| N/N1 + N/N2 | P1 (Phoenix embedding only) | Three factor sources on one `/api/v1/graphql` when the kernel is embedded in `api.js` without `ins-native`: set 1 (executionAnalytics), set 1 strict (grouped roots), embedded float table (`analytics.ts`, JS `1 / 5.05` bound as numeric) for rankings/heatmaps/timeseries and the budget MCP tools. Dead on Chronos dev, live on Phoenix dev. Owner rule "embedded 2026 estimates must not survive native admission" is violated on that path.                                                                                                     | `src/modules/budget/shell/repo/analytics.ts:24-83`, `money-factor.ts:78-104`, `src/app/build-app.ts:1222`                                       |
| N/N3        | P1 (ops)                    | Population admission pins (POP107D revision/custody/transform SHAs, eight sector source strings with object versions, `rowsSha256`, three hard-coded year-set shapes) live in `src/app/`. Any scrapper re-publication fails closed, and the blast radius includes **nominal** entity rankings and the nominal entity table (they call the population relation for metadata), not only per-capita. Sector admission is re-validated on every population read regardless of scope. Acceptable for dev, not for prod without a DB-side promoted pointer. | `src/app/native-map-population.ts:26-60`, `native-sector-population.ts:44-115`, `budget-repo.ts:1366-1371`, `grouped-analytics-repo.ts:324-331` |
| I/INS-03    | P2                          | Fail-open composition in `build-app.ts`: any error mounting the redesign surface is logged and the app continues legacy-only, silently dropping `/api/v1/graphql` and `/api/v1/mcp`. Adding `ins-native` to the embedded defaults while the interim INS slice is mounted would collide on `Query.insDatasets` and take exactly this path.                                                                                                                                                                                                             | `src/app/build-app.ts:1215-1239`                                                                                                                |
| X/F7        | P2                          | `CLERK_SECRET_KEY` on the Chronos secret builds the Clerk write-permission checker, which needs `api.clerk.com`; the namespace denies internet egress (lead verified the policy set), so every uncached public-write check fails closed after a 5 s timeout.                                                                                                                                                                                                                                                                                          | `src/app/native-map-routes.ts:47-55`, `advanced-map-datasets/shell/security/clerk-write-permission-checker.ts`                                  |
| X/F2        | P2                          | `/api/v1/ready` gates on the single-instance, no-backup dev user DB; its outage removes both API pods from the Service and takes the public read API down.                                                                                                                                                                                                                                                                                                                                                                                            | `src/app/build-redesign-app.ts:873-882`                                                                                                         |
| N/M1        | P2                          | Native map mount applies `makeAuthMiddleware` scope-wide, including the three public GETs; a present expired token → 401, a Clerk outage → 503, where the legacy app bypassed provider verification on those prefixes (pinned by `tests/unit/app.test.ts:1508`).                                                                                                                                                                                                                                                                                      | `src/app/native-map-routes.ts:88`                                                                                                               |
| N/M3        | P2                          | Legacy `mcp` module's Zod filter schema strips `isTerritorialExecutive` silently → unfiltered results with no error. Advanced-map now carries it end to end; notifications pass it opaquely; report loaders have no filter path.                                                                                                                                                                                                                                                                                                                      | `src/modules/mcp/core/schemas/zod-schemas.ts:97-106`                                                                                            |
| X/F8, X/F9  | P2 (legacy path)            | `setStatementTimeout` issues `SET LOCAL` outside a transaction at all 20 call sites (a no-op), and the three Phoenix pools have no `pool.on('error')` (the tunnel-drop crash). Kernel and user pools are correct.                                                                                                                                                                                                                                                                                                                                     | `src/infra/database/query-builders/timeout.ts:72`, `src/infra/database/client.ts:31-39`                                                         |
| B/F5        | P2                          | Two identity policies coexist on the kernel: grouped `entityAnalytics` requires a servable identifier and a public-or-absent organization, while `rankEntitiesPage`, `rankCommitmentEntities` and report listings emit `core.public_entities` names ungated. Not a leak today: `public_entities` has no `privacy_class` column and zero public entities map to a restricted organization (D §M3), but the e2e fixture treats the case as possible.                                                                                                    | `src/modules/budget/shell/repo/budget-repo.ts:1318,1465-1470,1833,1873`                                                                         |
| B/F3        | P2                          | Null/zero per-capita population now aborts with `ServiceUnavailable` (legacy served unadjusted). Undocumented delta; and because `SERVICE_UNAVAILABLE` is not in `SAFE_ERROR_CODES` the message becomes `Internal server error` in production (B/F12; lead verified the set).                                                                                                                                                                                                                                                                         | `budget/core/legacy-analytics/usecase.ts:81-83`, `src/infra/graphql/security.ts:90-99`                                                          |
| B/F15       | P3                          | `rankCommitmentEntities` orders by the un-coalesced sum while selecting the coalesced one; `limit` silently clamped and `year` unvalidated, unlike `rankEntitiesPage`.                                                                                                                                                                                                                                                                                                                                                                                | `budget-repo.ts:1458-1471`                                                                                                                      |
| N/R3        | P3                          | `isTerritorialExecutive: Boolean!` mapped raw; a NULL nulls the whole node.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `reference/shell/graphql/mappers.ts:63`                                                                                                         |
| N/R2        | P3                          | `normalizeCui` accepts 13 digits while the withheld-identifier check rejects >10, so a CNP-shaped `referencePublicEntity(cui:)` reaches the table (returns nothing today).                                                                                                                                                                                                                                                                                                                                                                            | `reference/shell/repo/public-entity-repo.ts:187-198`                                                                                            |
| I/INS-06    | P3                          | INS fact pages use `limit/offset` on the partitioned fact table with a sort key no index covers; MCP accepts offsets up to 2^31.                                                                                                                                                                                                                                                                                                                                                                                                                      | `ins-native/shell/repo/ins-repo.ts:919-925`                                                                                                     |
| I/INS-11    | P3                          | `ins.territory_nodes`, `ins.contexts`, `ins.dataset_coverage` carry `privacy_class` (100 % public today) that the server never filters or types.                                                                                                                                                                                                                                                                                                                                                                                                      | `ins-native/shell/repo/territory.ts:34-38`                                                                                                      |

### 2.1b Other source modules, interfaces, MCP, auth (from modules-interfaces.md)

The modules agent reviewed with no DB access and rated four privacy gaps P1
"unless the count is zero". The lead ran every named owner-check count on Chronos
on 2026-09-09; **all are zero**, so these are gate-absent-but-latent (P2), not
exposures. The privacy discipline rule still applies: the gate belongs in the SQL,
not in the data.

| ID                                      | Sev                                        | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Lead verification                                                                                                                                         |
| --------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M/M01                                   | P2 (latent)                                | `legal.document_nodes` outline and `get_legal_node` reads carry no `privacy_class` predicate (only `graph-repo.ts:303` edges and `render-repo.ts:70` render are gated); `entryByPath` accepts any disposition.                                                                                                                                                                                                                                                                                                     | `count(*) where privacy_class <> 'public' and role is null` = **0** of 110,534,020 nodes; code grep confirms the predicate is absent in `outline-repo.ts` |
| M/M02                                   | P2 (latent)                                | Eleven `core.organizations` reads in `companies-repo.ts` pin no `privacy_class`; the only guard is `length(cui) <= 10`, which 47cd244a's own message says must not be relied on. The kernel pins all seven of its own reads.                                                                                                                                                                                                                                                                                       | `kind='company' and privacy_class <> 'public' and length(cui) <= 10` = **0**                                                                              |
| M/M07                                   | P2 (latent)                                | Render gate uses chunk 0's `privacy_class` only; chunk N is never re-checked.                                                                                                                                                                                                                                                                                                                                                                                                                                      | `document_render where chunk_index > 0 and privacy_class <> 'public'` = **0**                                                                             |
| M/M18                                   | P2 (latent)                                | `core.territories.privacy_class` untyped and unpinned in 5 of 6 kernel territory reads (raw-SQL map paths pin it).                                                                                                                                                                                                                                                                                                                                                                                                 | `territories where privacy_class <> 'public'` = **0**                                                                                                     |
| M/M16                                   | P2 (latent)                                | `hasFinancials` filter predicate lacks the `privacy_class` allowlist the row reads have (presence leak).                                                                                                                                                                                                                                                                                                                                                                                                           | `companies_v2.financials where privacy_class <> 'public'` = **0**                                                                                         |
| M/M02                                   | `d95a33d7`                                 | All eleven `core.organizations` reads gated with the kernel helper; the shared list predicate covers rows, total and both aggregate scans.                                                                                                                                                                                                                                                                                                                                                                         |
| M/M05                                   | P1 (test)                                  | `servableDocumentRowSql` references `search.documents.privacy_class`, but the Kysely type omits it and the e2e DDL creates the table without it, so `search-repo-entities` must fail wherever e2e runs. Lead verified the DDL (`tests/e2e/search-repo-entities.test.ts:76-93` has `visibility`, no `privacy_class`). Live DB has the column (D §M1).                                                                                                                                                               | code confirmed                                                                                                                                            |
| M/M03                                   | P2 (security)                              | Both entrypoints run `trustProxy: true` (`api.ts:169` defaults to true with `TRUST_PROXY` unset everywhere; `build-redesign-app.ts:215` hard-codes it), so `request.ip` is the leftmost client-controlled `X-Forwarded-For` and the anonymous MCP / map / search rate buckets are bypassable by header rotation. Lead verified the three code sites and that no manifest sets `TRUST_PROXY`. Istio header behaviour unverified.                                                                                    | code confirmed                                                                                                                                            |
| M/M04                                   | P2 (conditional)                           | Legal render responses carry `s-maxage=86400` + SWR on a URL without a generation component; a shared cache can serve a document up to ~48 h after a public→restricted flip. Whether a shared cache sits in front is unverified.                                                                                                                                                                                                                                                                                   | code confirmed                                                                                                                                            |
| M/M06                                   | P2 (known since 2026-07-12, unactioned)    | `PROCUREMENT_DATA_AVAILABILITY.frameworkRole` is a compile-time `false` while the build id is a runtime value; the next data build that publishes `framework_role` keeps ceilings in totals with no signal. This is the F1 gate-flip recorded in the 2026-07-12 procurement design review; no commit since has addressed it. Also M/M21: the guard rejects `frameworkRole: 'all'`, the documented no-op.                                                                                                           | code confirmed by agent; ClickHouse is off on Chronos dev (`PROD_CLICKHOUSE_URL=''`) so not live there; listed again in §2.5                              |
| M/M15                                   | P2                                         | `retrieval-repo.ts:174` outer `order by in_degree desc, section_key asc` re-introduces the tie 75409f0b closed two lines above (79.7 % of candidates tie at in_degree 0).                                                                                                                                                                                                                                                                                                                                          | agent                                                                                                                                                     |
| M/M13                                   | P2                                         | The GraphQL merge gate records field owners only for kernel base types, so a cross-module `extend type` on a module-owned type passes the gate and fails at boot.                                                                                                                                                                                                                                                                                                                                                  | agent                                                                                                                                                     |
| M/M09                                   | P2                                         | Same path `/api/v1/graphql`, different INS root sets on the two entrypoints (interim serves `insCompare`/`insUatIndicators`; native lacks them). Same fact as I/INS-01.                                                                                                                                                                                                                                                                                                                                            | lead probe confirms                                                                                                                                       |
| M/M10, M/M11                            | P2 (contract)                              | Procurement registers no `SourceContributor` (entity-360 never reports procurement). REST exists for three modules only (pnrr full, parliament 1, legal 2) and no redesign route is in any OpenAPI; the "REST deferred" note in `build-redesign-app.ts:9` has no recorded decision in `docs/`.                                                                                                                                                                                                                     | agent; contract matrix in the file                                                                                                                        |
| M/M12                                   | P2 (test)                                  | No CI test stitches the real ten-module schema and there is no SDL snapshot of `/api/v1/graphql` (171 roots, 907 types, 6 `@deprecated`, no Mutation at HEAD, built offline by the agent).                                                                                                                                                                                                                                                                                                                         | agent                                                                                                                                                     |
| M/M08                                   | P2 (LLM-facing)                            | Search degrade path is honest in code, but the MCP summary, SDL and widget still promise an exact-CUI lookup that 47cd244a removed, and report `engine: 'postgres'` on a path that runs no query.                                                                                                                                                                                                                                                                                                                  | agent                                                                                                                                                     |
| M/M17, M/M20, M/M19, M/M30, M/M39–M/M42 | P2–P3                                      | Companies pagination without a total order on NULL-cui rows; `ParliamentMemberVote.billKey` not migrated and `billKey` deprecated in prose only; `search_legal_acts.includeSuspicious` accepted and ignored; inconsistent clamp-vs-refuse on `first`; `legalActs.totalCount` swallows errors; `incomingAnchors` cursor differs from `links`; forged cursor → masked 500 in `listActs`; false `degraded: true` on the companies pg fallback.                                                                        | agent                                                                                                                                                     |
| M/M23                                   | decision recorded 2026-07-12, not executed | The decision to delete the legacy `mcp` module and GPT REST has not moved (0 commits; still mounted and auth-bypassed on `api.ts`); listed again in §2.5.                                                                                                                                                                                                                                                                                                                                                          | agent                                                                                                                                                     |
| M/M24, M/M32–M/M35, M/M37, M/M38        | P3                                         | `legal/mo/` is a third layout outside the ESLint boundary patterns; module factories disagree on shape (`restPlugin` vs `routesPlugin`, async `makeLegalModule`); `asOf` typed six ways, `totalCount` nullable inconsistently, three `SortDir` clones; 13 copies of `toGraphqlError` and per-module filter helpers that belong in `shared/`; companies encodes a page index inside the keyset cursor envelope; kernel cursor decoder still uses raw `JSON.parse`; 22 files over 800 lines (parliament repo 4,676). | agent                                                                                                                                                     |
| M/M26                                   | P3                                         | Committed widget bundles (`*.gen.ts`) are rebuilt by nothing in CI.                                                                                                                                                                                                                                                                                                                                                                                                                                                | agent                                                                                                                                                     |

### 2.2 Upstream data defects the server exposes

| ID                 | Sev                            | Finding                                                                                                                                                                                                                                                                                                                                                                                                                 | Evidence                                                                                                                                                 |
| ------------------ | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D/DP-01            | P1                             | Double-counted annual expense on main-creditor handover (see verdict 3). Server mitigation until the rebuild: detect `count(distinct main_creditor_cui) > 1` per (entity, year, report type) in the annual MV and either caveat the figure or refuse to collapse; add a golden assertion on 26429279/2024.                                                                                                              | Lead SQL: Phoenix flags only month 12 (2,049,760,834.64); Chronos flags month 6 (869,074,554.38) and month 12; handover scopes 2023/2024/2025 = 115/1/54 |
| D/DP-03            | P2                             | 26 fact-bearing CUIs (2023: 21, 2024: 5; 57.6 M / 3.1 M RON) have MV rows but no `core.public_entities` row: their money appears in rankings and aggregates but their entity pages 404. 251 Phoenix entities without a UAT are absent from Chronos `public_entities` (present only as `core.organizations kind='unknown'`).                                                                                             | D §M4                                                                                                                                                    |
| D/DP-13            | P3                             | Chronos gives no `is_yearly` row to entities whose YTD chain is all zero (Phoenix flags their last month); entity counts differ (2025 detailed 12,987 vs 13,592) with no money impact.                                                                                                                                                                                                                                  | D §M4                                                                                                                                                    |
| D/DP-09            | P3                             | Chronos keeps ~533 k more 2024 detailed fact rows (269 k zero-amount continuity rows, program-code grain) with identical sums; line-listing counts differ from Phoenix.                                                                                                                                                                                                                                                 | D §M4                                                                                                                                                    |
| D/DP-05            | P2 (ops)                       | A login-capable write-lane role `commitments_import_20260909` with two idle sessions exists on the serving DB today, not yet recorded in the TRACKER.                                                                                                                                                                                                                                                                   | D §M8                                                                                                                                                    |
| I/INS-04, I/INS-05 | P2 (perf, scrapper-side index) | Every INS modern-scope predicate bitmap-scans a dataset's whole `dataset_geo_tuples` and filters by territory (3,223 rows removed per territory per dataset); catalog reads re-validate publication custody over 209 k tuples per request (286 ms for one dashboard discovery). A `(dataset_code, territory_id) where resolution='EXACT'` index and a per-dataset publication stamp are the two highest-leverage fixes. | I §B6 EXPLAIN                                                                                                                                            |

### 2.3 Scope gaps versus the plan (doc 13)

| ID           | Finding                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B/F1, T/T-09 | Heatmaps, commitments roots, `entities`, `uats`, `datasets`, `staticChartAnalytics` are not on the kernel. Native replacements exist under other names (`budgetCountyHeatmap`, `budgetCommitment*`, REST map routes). Doc 13 §5 step 7 (retire legacy) cannot run until this is decided.                                                                                                                           |
| B/F6         | If the commitments roots are ported: the kernel commitment metric enum exposes 4 of the legacy 14 values for timeseries/ranking, and `total_plati` / `execution_rate` / `commitment_rate` have no kernel counterpart (fact-path metric families are complete after 08a7fb6b).                                                                                                                                      |
| I/INS-02     | `ins-native` has no REST slice; the binding contract requires REST + GraphQL + MCP per source module.                                                                                                                                                                                                                                                                                                              |
| X/F10        | Six client route families are not mounted on Chronos dev (`/api/v1/admin/campaigns`, `/api/v1/notifications*`, `/api/v1/learning/progress`, `/api/v1/short-links`, `/api/v1/agent*`, `/api/v1/campaigns*`); whether the client guards them on dev-chronos is unverified.                                                                                                                                           |
| I/INS-01     | The default image (`dist/api.js`) still serves the legacy Phoenix INS module on `/api/v1/graphql` and hard-requires `INS_DATABASE_URL`, while the current client tree sends native-only inputs. Latent today because Phoenix dev runs a frozen client (6681754d) against a frozen server (b80c4778), lead verified both live images; it becomes live the moment a current client is pointed at an `api.js` server. |

### 2.4 Verification gaps (what nobody has proven yet)

| ID              | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T/T-01..T-03    | Golden master: wrong dev hostname, `continue-on-error` on both jobs, runs from `main` so `dev` edits are inert. Zero green Dev jobs in 400 runs.                                                                                                                                                                                                                                                                                                           |
| T/T-04, X/F11   | No CI job runs `tests/e2e` (51 files): the only real-Postgres proofs of executionAnalytics on the kernel, grouped roots, ins-native (146 cases), factor-set pin, owner-deletion fence and borrowed transactions run only on a laptop. Two files (`normalization-factor-set`, `map-owner-deletion`) can never run in CI as written (loopback-only DB URLs, no container fallback).                                                                          |
| T/T-05          | No test executes a GraphQL document against `executionAnalytics` on the kernel with fakes; argument coercion, `PeriodDate` parsing and the error envelope are covered only by the local cutover replay and one live-URL GM spec.                                                                                                                                                                                                                           |
| I/INS-07, T §CI | The 23 INS cutover "passes" of 2026-09-02 were recorded against the interim Phoenix mount; `entityAnalytics`/`aggregatedLineItems` have no parity evidence. Native INS and grouped-root parity on live data was unmeasured at the time of the agents' reports. **Lead action:** the cutover replay was started 2026-09-09 against `api-dev.transparenta.eu/graphql` (baseline) vs `dev-chronos-api.transparenta.eu/api/v1/graphql` (target); result in §5. |
| T/T-08          | `pnpm gm:corpus:check` crashes against the current client tree; the corpus pins client `6681754d` (which is what Phoenix dev runs, so the replay is still meaningful).                                                                                                                                                                                                                                                                                     |
| T/T-11          | 226 of the 227 skipped tests are the ten `*-golden` suites gated on `PROD_DATABASE_URL`, which CI never sets.                                                                                                                                                                                                                                                                                                                                              |
| T/T-15          | CodeQL scans `main` only; all migration code is on `dev`.                                                                                                                                                                                                                                                                                                                                                                                                  |
| N/T1, T/T-13    | No test pins the set-2 digest or `makeNativeBudgetFactors`; the four shell population readers and `readMapPopulationAnchorSets` are referenced by no test; no same-year cross-root factor equality test.                                                                                                                                                                                                                                                   |

### 2.5 Decisions to confirm (owner calls, not bugs)

| ID               | Question                                                                                                                                                                                                                                                                                                                                                 | Current state                                                                                                                                                                                                                                |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B/F2, N/N4, N/N5 | On Chronos dev `executionAnalytics` runs the native strict series (set 2, exact-year factors, missing periods reported, ancestor-suppressed scopes, INS national node as country denominator). Doc 13 §7 "not deltas" and §9 "pinned to set 1" describe the fallback, not the deployment. TRACKER 2026-09-08 records the gap-aware contract as accepted. | Confirm the accepted policy and rewrite doc 13 §7/§9 as a deployment matrix (native strict on the standalone kernel; legacy policy only on the `api.js` embedding until retirement).                                                         |
| D/DP-02          | The 41 county councils are `is_uat=true` on Phoenix but `is_uat=false, is_territorial_executive=true` on Chronos (lead verified). Any `isUat=true` filter on Chronos drops them; per-capita paths already use the executive flag, plain `isUat` filters do not.                                                                                          | Decide whether `isUat` maps to `(is_uat or is_territorial_executive)` in the three predicates, or document the semantic change for the client.                                                                                               |
| D/DP-04          | Entity-type vocabulary changed (`admin_municipality` → `uat`, etc.) and the reference API exposes Chronos values raw; legacy grouped typedefs still mention the old values.                                                                                                                                                                              | Confirm the taxonomy and add a legacy→Chronos mapping or version the filter.                                                                                                                                                                 |
| B/F1             | Port the five legacy roots with frozen SDL, or amend doc 13 to "replaced by native roots; client migration is the gate".                                                                                                                                                                                                                                 | Undecided in docs.                                                                                                                                                                                                                           |
| N/M1, N/M2       | Native map routes: auth middleware on public GETs; one 30/min per-IP bucket across all 20 routes; no result cache (every public map view re-runs the 30 s aggregation).                                                                                                                                                                                  | Decide the public-read policy for prod.                                                                                                                                                                                                      |
| B/F4             | Two additive fields on frozen types (`AnalyticsFilterInput.is_territorial_executive`, `AnalyticsSeries.missingPeriods`) with only a test-side carve-out; doc 13 §1 allows `@deprecated` only.                                                                                                                                                            | Record the Appendix-B decision or revert.                                                                                                                                                                                                    |
| X/F3             | `k8s/base/*` is not frozen for Phoenix dev: its Argo app auto-syncs base edits with prune + selfHeal.                                                                                                                                                                                                                                                    | Pin the Phoenix dev app to a revision for the migration window, or give chronos-dev its own base.                                                                                                                                            |
| N/N3             | Population admission pins (POP107D revision/custody/transform SHAs, eight sector sources, `rowsSha256`, year sets) are hard-coded in `src/app/` and re-validated on every read; a scrapper re-publication fails closed, including nominal rankings and the nominal entity table.                                                                         | Confirm the pin-in-code strategy is intended through prod, or accept the recommendation: a DB-side promoted pointer (like `core.factor_sets.promoted_at` + digest) read at request time, with nominal roots degrading to `population: null`. |
| M/M06, M/M23     | Two 2026-07-12 decisions with no commits since: the procurement `frameworkRole` gate-flip guard, and deletion of the legacy `mcp` module and GPT REST.                                                                                                                                                                                                   | Re-confirm, schedule, or record the deferral with a date.                                                                                                                                                                                    |

### 2.6 Documentation staleness (load-bearing rows only; full sweep in X §"Doc staleness sweep", 25 rows)

- `AGENTS.md:77` "`src/api.ts` is what ships" — false for Chronos dev; `ins-native` absent from the module map; `ins` listed as a source module.
- `docs/server-redesign/13-legacy-roots-on-kernel.md`: "DRAFT r1" header vs r2 body and a 09-05 amendment; §2/§4 assign unported roots; §3 rule 3 and §4 row 2 describe an MV path `entityAnalytics` no longer uses; §7 "not deltas" contradicted by search broadening (B/F9), null-population abort (B/F3), the standalone-kernel policy (B/F2); §9 set-1 pin stale for Chronos dev; PageInfo clone has three fields not two (B/F10); dimension-root deltas recorded only in code comments (B/F11).
- `TERRITORY_CHRONOS_DEV_ROLLOUT_2026-09-05.md`: "No territory L2 apply" and "Remaining prerequisites before L2" precede "L2 is now published"; stale pin `77551935`.
- `NATIVE_GROUPED_ANALYTICS_2026-09-05.md:48` "no annual adapter is selectable yet" — superseded by 4036d387/6d9bef23/56ccb941.
- `INS_MAP_SERIES.md:3` "not yet mounted on public map routes" — mounted (f905539d).
- `src/app/ins-interim-surface.ts:7-8` "INS kernel module does not exist yet".
- `docs/DEPLOYMENT-SPEC.md:545` wrong CMD; `k8s/README.md:199-207` "base frozen" false; Chronos Argo apps live outside this repo.
- `docs/GOLDEN-MASTER-E2E-SPEC.md` §6.2 and `.claude/skills/verify-and-ship/SKILL.md` describe a CI that does not exist.
- `docs/USER-DATA-ANONYMIZATION.md`: Chronos dev store covered behaviourally but unnamed, and the two 2026-09-06 sections sit below "References" (X/F23).
- `src/infra/database/budget/schema.sql` vendors a Phoenix `set_period_flags` that groups by main creditor, which live Phoenix does not do (D/DP-12); this is how DP-01 went unnoticed.

### 2.7 Hygiene and code-quality improvements

| ID                                     | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| X/F5, X/F6                             | ESLint `boundaries` classifies only `src/app.ts` and `src/api.ts` as `app`, so `src/app/**` is unchecked: `build-app.ts` (5), `build-redesign-app.ts` (1) and `cache-wrappers.ts` (30 type imports) reach module internals. The 12 `src/app/native-*.ts`/`ins-*.ts` files hold factor pins, admission SHAs, sector SIRUTA rules, BigInt/Decimal roll-ups and a duplicated `jsonb_to_recordset` SQL fragment, with zero unit tests under `tests/unit/app/`. Consolidation proposal in X §"Consolidation proposal": an `AnnualPopulationPort` in `shared/core/ports`, the adapter and admissions owned by `ins-native`, `budget` consuming the port, `src/app/` reduced to wiring. |
| X/F4                                   | CI image-pin commit pushes to `dev` without fetch/rebase under `cancel-in-progress`: 15 "Merge remote-tracking" commits, pinned trees never CI-validated, `HUSKY=0` skips the secret scan for that commit. Fix: rebase-with-retry before push, or pin on a deploy branch Argo tracks.                                                                                                                                                                                                                                                                                                                                                                                            |
| N/N6, N/N7                             | Native adapters are optional with silent `??` fallbacks to the set-1/embedded composition; `legacyFactors` is built without `requirePromotion`; `fxPolicy` defaults to `'period'` on the compat path and `'cpi-base-year'` on native. Make the native deps required when `budget` is enabled, or fail startup without `ins-native`.                                                                                                                                                                                                                                                                                                                                              |
| B/F13, B/F14, B/F16, B/F19             | Legacy repos interpolate driver `error.message`; legacy/grouped transactions lack `setAccessMode('read only')` and the `57014` → Timeout branch the map repos have; grouped resolvers drop `extensions.field`; `requiredText` throws inside the mapper and surfaces as a DB error.                                                                                                                                                                                                                                                                                                                                                                                               |
| B/F18, N/P3                            | Duplicated factor-eligibility block (core vs shell), duplicated `inList`/`notInNullSafe` with different empty-list semantics, four copies of the "preload factors before borrowing a connection" pattern in `src/app/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B/F21, I/INS-16, T/T-17                | Oversized files: `budget-repo.ts` 2,410 lines (+929 this window), `ins-repo.ts` 958, `ins-native` resolvers 771, `build-app.ts` 2,757; 24 test files > 1,000 lines. Two full INS implementations coexist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| N/P5, D/DP-11, D/DP-15                 | Dead-but-compiled: YAML `FactorSource` adapter, CPI chain-linker, `factorValuesCte`, the embedded float table, Phoenix `SUM(DISTINCT population)` denominators. Delete with the legacy entrypoint; until then `@deprecated`, and convert the embedded table to decimal strings aligned to set 2.                                                                                                                                                                                                                                                                                                                                                                                 |
| N/K1                                   | Kernel (`shared/`) hard-codes Bucharest county `'B'`, SIRUTA `179132`, sector kind rules; `core.territory_population`, `core.territory_identifiers` and `territories.privacy_class` are used in raw SQL but missing from the Kysely types (also D/DP-07: `privacy_class` untyped on six tables).                                                                                                                                                                                                                                                                                                                                                                                 |
| X/F13, X/F16                           | Procurement/legal search and ClickHouse settings bypass the TypeBox `RedesignEnvSchema`; the Chronos ConfigMap carries nine keys the entrypoint never reads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| X/F14, X/F15                           | Public `/api/v1/health` and `/ready` return raw dependency `error.message`; `trustProxy: true` is hard-coded and rate limits key on `request.ip` (gateway header handling unverified).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T/T-10, T/T-16, T/T-12, T/T-21, T/T-22 | `vi.mock` in 4 files and `vi.spyOn` in 5 despite the no-mocking rule (the two legacy integration suites mock the repo away); `makeCapturingDb` copied into 14 files; `eslint-boundaries.test.ts` is a known load flake (15 s cap, 9.6 s idle); `format:check` fails on 75 files; `vitest.config.ts` sets no timeout/worker limits.                                                                                                                                                                                                                                                                                                                                               |
| X/F24, X/F18, X/F19, X/F26             | Repo hygiene: tracked `prompt.md`, ten ignored `tmp-*.ts`, `app.log`, `.DS_Store` in ten tracked dirs, `security-tests/` empty while two `package.json` scripts point into it, a dangling `gitops-canary` Argo app on Chronos, duplicate `pnpm build` in CI, image `chown 65532` vs pod UID 1001.                                                                                                                                                                                                                                                                                                                                                                                |
| I/INS-10, I/INS-12, N/M4, B/F20        | `ins-native/core/entity-context.ts` value-imports from the shared barrel (lint allows it); `registry` dep required but unused; deep import of `auth/shell/middleware`; budget shell type-imports from the legacy `normalization` module scheduled for deletion.                                                                                                                                                                                                                                                                                                                                                                                                                  |

## 3. What was verified clean (coverage statement)

- Schema usage: every table and column the kernel code references exists in Chronos with a compatible type; MV column sets match the typed interfaces; no missing relation.
- Lineage: execution report counts per year × type are identical on both sides for 2023–2025; the two aggregated report types reconcile to the cent; 2024 detailed YTD and monthly sums are identical to the cent (5,107,425,783,770.51 / 833,522,344,758.26); Cluj-Napoca 2024 identical; county population 19,053,815 identical; dimension labels identical; commitments 2023/2024 identical; 2025 commitments differ because Phoenix's MV is stale, Chronos is ahead.
- Factors: set 2 promoted (run 23230), digest equals the code pin, reader validates digest/kinds/period keys/positivity on every call, mutable pointer never cached; native lookup errors never fall through to embedded rates.
- Population: exact-year POP107D + admitted sector rows; division before sum; fail-closed on missing year or nonpositive population; latest-selected-year Population column (user decision 2026-09-08); county population from the county node, never a sum; the Bucharest county node now exists in prod; the eight sector sources match the DB one-to-one.
- INS: 42/42 counties resolve through the canonical alias join with zero conflicts; 3,181/3,183 LAU nodes linked (the two unlinked are the known residual); POP107D custody pin matches live; sector admission rows exist for all eight pinned years; SDL for the eight ported roots byte-identical to legacy except documented additive fields.
- Read-only: only the serving-DB read role has live sessions from the app pattern; every candidate role has `default_transaction_read_only=on`; no write builder targets the prod pool; writes go only to the chronos-dev user DB.
- Auth and user data: borrowed-transaction fix correct; deleted-owner fence and anonymizer share the lock; Chronos dev `user.deleted` path composes the same anonymizer over the same 30-table schema; Clerk dev instance isolated from prod; JWT verified locally with issuer and `azp`; no hard-coded secrets.
- Deployment: pod security (non-root, read-only FS, no SA token, seccomp), anti-affinity, PDB, sealed secrets only, cosign-verified base image, Trivy gate.
- Rules across all reviewed modules: no `throw` in `core/`, no float on money on the Chronos path, no raw `JSON.parse`, no cross-source internal imports, kernel imports no source module, MCP tools annotated read-only globally.

## 4. Recommended order of work

1. **Data:** scrapper fixes `is_yearly` per (entity, year, report type, sector) or dedups creditor handovers in the MV (D/DP-01); admit the 26 fact-bearing CUIs to `core.public_entities` (D/DP-03); add the `(dataset_code, territory_id)` INS index and a per-dataset publication stamp (I/INS-04/05); record and close the `commitments_import_20260909` lane (D/DP-05).
2. **CI truth:** fix the GM hostname, drop `continue-on-error`, run GM from `dev`, add the cutover replay against both dev targets, add an e2e job with Postgres 18 + pinned scrapper checkout, run CodeQL on `dev` (T/T-01..04, T-15). Rebase-with-retry or deploy-branch for the image pin (X/F4).
3. **Prod-readiness bugs:** probe timeouts (X/F1), readiness gating (X/F2), Clerk egress or drop the key (X/F7), public map GET auth scope (N/M1), move population admission to a DB-side promoted pointer and let nominal roots degrade instead of fail (N/N3), fail-closed boot on redesign mount errors (I/INS-03), add `SERVICE_UNAVAILABLE` to the safe codes (B/F12).
4. **Decisions:** §2.5 rows, then rewrite doc 13 as a deployment matrix and close the 25 stale doc rows.
5. **Retirement path:** delete the embedded float table and the `??` fallbacks (N/N1/N2/N6), move `FactorSetReader` into the kernel (B/F20), enforce boundaries on `src/app/**` and consolidate the native composition into modules (X/F5/F6), delete the legacy INS module once INS-01 is decided, split `budget-repo.ts`.
6. **Tests:** the ten-item list in T §"Improvements" (kernel-legacy-roots integration suite with the corpus, owner-fence unit test, Testcontainers fallback for the two loopback-only e2e files, capturing-driver SQL tests for `ins-repo.ts`, population reader unit tests, set-2 pin test).

## 5. Lead cross-checks performed on 2026-09-09

| Check                                                                                  | Result                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Chronos dev entrypoint and env                                                         | `args: dist/redesign-api.js`; no Phoenix DB or Redis env names on the pod (overlay + infra agent live read)                                                                                                                                                                                                                                                              |
| Factor pins in code vs DB                                                              | code: legacy set 1 digest `69cc0473…`, native set 2 digest `5f2948ec…`; DB `core.factor_sets`: set 1 unpromoted, set 2 promoted 2026-09-08 run 23230; digests equal                                                                                                                                                                                                      |
| `executionAnalytics` routing on Chronos dev                                            | `deps.executionSeries ?? legacyExecutionSeries` in `budget/shell/graphql/legacy/resolvers.ts:179`; `build-redesign-app.ts:436` passes the native series when `ins-native` is enabled (default on the standalone entrypoint)                                                                                                                                              |
| DP-01 reproduction                                                                     | Chronos annual MV 26429279/2024 detailed: 869,074,554.38 (creditor 4283422) + 2,049,760,834.64 (self); Phoenix flags only month 12 (`is_yearly=t`), June `f`; Chronos monthly MV shows August YTD 1,305,581,686.29 under the new creditor, i.e. the YTD chain continued and the June row is a duplicate                                                                  |
| DP-01 exposure                                                                         | multi-creditor (year, entity, report type) triples in the annual MV: 6,276 / 6,151 / 5,689 for 2023/2024/2025; of those, disjoint month ranges (true handover): 115 / 1 / 54; the rest overlap (parallel reporting, e.g. entity 10065309 under creditors 13729380 and 2540813 every month) and are not double counts                                                     |
| DP-02                                                                                  | `core.public_entities` joined to territories, `is_territorial_executive=true`: county 41 rows `is_uat=f`; locality 6 `t`; uat 3,181 `t`                                                                                                                                                                                                                                  |
| Root presence by endpoint                                                              | Phoenix dev `/api/v1/graphql`: `executionAnalytics` and `budgetEntityRanking` present, `entityAnalytics` absent; Chronos dev `/api/v1/graphql`: `executionAnalytics` and `entityAnalytics` present; both lack `heatmapCountyData`, `commitmentsSummary`, `entities`, `uats`, `datasets`; Phoenix `/graphql` has all legacy roots; introspection disabled on both kernels |
| Live Phoenix dev images                                                                | server `b80c4778…`, client `6681754d…` (= GM corpus pin); Phoenix dev client `VITE_API_URL` = `api-dev.transparenta.eu`; Chronos dev client at `8236549a…` → `dev-chronos-api.transparenta.eu`                                                                                                                                                                           |
| GM last run (34326354425, 2026-09-09 07:55Z)                                           | workflow `success`; jobs: Golden Master (Dev) `failure`, Golden Master (Prod) `failure`, Summary `success`; `DEV_API_URL` default `dev-api.transparenta.eu`; `continue-on-error: true` at lines 62 and 109                                                                                                                                                               |
| Probe timeouts                                                                         | base deployment startup/liveness `timeoutSeconds: 3`; `shared/index.ts:221-223` per-dependency `setTimeout(…, 4000)`                                                                                                                                                                                                                                                     |
| Egress policies in `transparenta-eu-dev`                                               | `default-deny` (Ingress+Egress), `allow-dns`, `allow-istio-control-plane`, `dev-user-database`, `client-api-egress`, `client-cloudflare-api-egress`; no server internet-egress rule                                                                                                                                                                                      |
| `SAFE_ERROR_CODES`                                                                     | UNAUTHENTICATED, FORBIDDEN, BAD_USER_INPUT, INVALID_INPUT, NOT_FOUND, RATE_LIMITED, GRAPHQL_PARSE_FAILED, GRAPHQL_VALIDATION_FAILED; no SERVICE_UNAVAILABLE                                                                                                                                                                                                              |
| Privacy owner-checks named by the modules review                                       | `legal.document_nodes` non-public with `role is null`: 0 of 110,534,020; `core.organizations` company, non-public, cui ≤ 10 chars: 0; `legal.document_render` chunk > 0 non-public: 0; `core.territories` non-public: 0; `companies_v2.financials` non-public: 0                                                                                                         |
| M/M05 e2e DDL                                                                          | `tests/e2e/search-repo-entities.test.ts:76-93` creates `search.documents` with `visibility` and no `privacy_class`; `document-privacy.ts` references `privacy_class`                                                                                                                                                                                                     |
| M/M03 trustProxy                                                                       | `src/api.ts:169` `config.server.trustProxy ?? true`; `build-redesign-app.ts:215` `trustProxy: true`; `TRUST_PROXY` set in no manifest or `.env.example`                                                                                                                                                                                                                  |
| Cutover replay (baseline Phoenix dev `/graphql`, target Chronos dev `/api/v1/graphql`) | 63 cases: 10 pass, 5 allowlisted, 48 fail; see §5.1                                                                                                                                                                                                                                                                                                                      |

### 5.1 Cutover replay result (lead run, 2026-09-09 08:38Z)

Per-case table: [cutover-replay-2026-09-09.md](./cutover-replay-2026-09-09.md).
Baseline Phoenix dev `/graphql` (server b80c4778, client corpus pin 6681754d), target
Chronos dev `/api/v1/graphql` (image 08a7fb6b). 63 cases: 10 pass, 5 pass with
allowlisted warnings, 48 fail. The 48 split as follows.

| Class                                                                                      | Cases | Reading                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root not on the kernel (`Cannot query field` / `Unknown type`)                             | 21    | scope gap B/F1: commitments ×4, datasets/staticChartAnalytics ×5, entities/uats pickers ×10, heatmaps ×2                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Native INS rejects the legacy dimension slug (`unknown classification dimension code SEX`) | 3     | `ins-native/core/usecases.ts:365` accepts only `D<n>` codes (declared delta I/INS-14). The pinned corpus client sends `SEX`; the Chronos dev client (8236549a) is native-aware. Any old client against the native slice breaks on the statistics series pages.                                                                                                                                                                                                                                                                   |
| `entityAnalytics` ×3                                                                       | 3     | `per_capita_amount` null-loss (approved nullable, NATIVE doc) + data drift; needs allowlist entries, none exist                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `aggregatedLineItems` ×2                                                                   | 2     | `totalCount` change + data drift (Chronos keeps unknown classification codes and fact labels); needs allowlist entries                                                                                                                                                                                                                                                                                                                                                                                                           |
| Classification pickers `totalCount`                                                        | 2     | S1-8 catalogue swap already allowlisted for the `*Names` documents but not for the paged picker documents                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Native INS value-level deltas                                                              | 14    | `totalCount` (-1 policy), `name_*_markdown` null, `sort_order` null, `metadata.flags`/`metadata.names` missing keys, county `territory.siruta_code` null, `periodicity` array order, plus data drift (Chronos has 2026 periods and revised 2025 values). I/INS-14 declares the markdown/`totalCount`/`path`/`parent_id` cases; **`metadata.flags`, `metadata.names`, `sort_order`, `periodicity` order and county `siruta_code` are not in the declared list** and need either a resolver fix or an explicit allowlist decision. |
| Data drift only                                                                            | 3     | `ins-dataset-dimension-values`, `ins-dataset-dimensions`, `statistics-uat-snapshot`: lineage (Phoenix Feb-2026 vs Chronos Sep-2026 publication)                                                                                                                                                                                                                                                                                                                                                                                  |

Net: the one corpus document for `executionAnalytics` (nominal RON income/expense
per entity, `execution-analytics-entity-income-expense`) passes on data and
envelope. No corpus document exercises a normalized (per-capita, EUR, inflation,
GDP) `executionAnalytics` request, so the set-1-vs-set-2 and denominator deltas
of N/N4 remain unmeasured by this replay. The four dimension roots pass or are
allowlisted; the INS territory roots pass. Everything else is either not ported,
awaiting an allowlist decision, or an undeclared INS value-level delta. This is the
first replay of the native INS slice and the grouped roots against live data; the
parity allowlist has no entries for any of them (T/T-08, I/INS-07 stand).

## 6. Notes on method and limits

- Subagents were launched with the default model (Fable) at default effort; the Agent
  tool exposes no effort setting, so "Fable high" applies to the lead session only.
- The Phoenix dev runtime composition was confirmed from live images, ConfigMap key
  names and endpoint probes, not by reading the pod's process.
- `rowsSha256` for the sector admission was not recomputed (I/INS-09), the POP107D
  `revision_id` pin was checked via custody digest only (N/N3), and the exact role the
  Chronos dev pool logs in as was inferred from session patterns, not the secret.
- GitHub API calls stopped after the rate limit was hit; the GM history numbers come
  from the tests agent's run before that.

## 7. Fix status (branch `fix/chronos-review-2026-09-09`, 2026-09-09)

Each row is one commit, reviewed by Codex (gpt-6-astra, high) before it was made;
every commit passed typecheck, lint, `deps:check` and the full unit+integration suite.
Two e2e suites ran on a throwaway Postgres (zeus Docker over an SSH tunnel): the budget
legacy-analytics suite (92/92, covers the commitment ranking and the territory repo)
and the search-repo suite (8/8). The legal, reference and companies changes are pinned
by compiled-SQL tests only; no e2e suite exercises those repos.

| Finding               | Commit                 | Note                                                                                                                                                                                                                                         |
| --------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| X/F1                  | `19ecc15d`, `8c8ab811` | Dependency-free `/api/v1/live`; the overlay commit must land **after** the CI image pin for the route, or the startup probe 404s.                                                                                                            |
| B/F3, B/F12           | `1eda0a86`             | `SERVICE_UNAVAILABLE` messages pass through; the one non-static emitter (`ask.ts`) made static.                                                                                                                                              |
| (found on the way)    | `ddeb3446`             | Under vitest `graphql` loaded twice (ESM + CJS); every production-redaction assertion in the redesign security suite passed on a cross-realm crash, not on the formatter. Pinned to one instance; assertions rewritten to the real contract. |
| N/R2                  | `6e59d6bd`             | Usecase refuses, repo fails closed; SQL-pinned.                                                                                                                                                                                              |
| B/F15                 | `64bdc9ee`             | Coalesced ORDER BY; year/limit validated like `rankEntities` (limit >100 now refuses instead of clamping — no client sends this root).                                                                                                       |
| M/M15                 | `f4a34893`             | Outer `act_id` tie-break.                                                                                                                                                                                                                    |
| M/M18, DP-07 (part)   | `401f51a9`             | Territory reads gated; `core.territories` and `search.documents` `privacy_class` typed.                                                                                                                                                      |
| M/M05                 | `0d3e1281`             | Fixture column + the measured P0A shape seeded; 8/8 on a throwaway Postgres.                                                                                                                                                                 |
| M/M16                 | `ce411e08`             | Both EXISTS branches.                                                                                                                                                                                                                        |
| M/M01                 | `93638465`             | Gate in the shared outline select.                                                                                                                                                                                                           |
| M/M07                 | `5c611d61`             | Requested row carries its own class; usecase answers `render_restricted` (403 kept distinct from 404).                                                                                                                                       |
| I/INS-03              | `5cd8ce49`             | Enabled surface that cannot mount fails the boot; test seam `registerRedesignContributors`.                                                                                                                                                  |
| N/M1                  | `2863e2fa`             | Public map GET/HEAD skip verification on the native mount, mirroring the legacy bypass.                                                                                                                                                      |
| M/M13                 | `874b8694`             | Module-owned fields recorded; both encounter orders.                                                                                                                                                                                         |
| X/F8, X/F9            | `e9604332`             | Legacy pools: 45 s pool-level bound (the largest per-repo value), error listeners; `setStatementTimeout` skips the discarded `SET LOCAL` outside a transaction.                                                                              |
| T/T-01..03 + Codex P2 | `85cbbd02`             | Hostname, blocking jobs, `dev` checkout, and a Chronos cutover-replay job (will be red until the parity allowlist records the known INS deltas).                                                                                             |
| T/T-15                | `79f69fe9`             | CodeQL on `dev` pushes; branch condition dropped.                                                                                                                                                                                            |
| X/F4                  | `a4745ecb`             | Rebase-with-retry before the pin push.                                                                                                                                                                                                       |
| M/M08                 | `a6145711`             | Degraded search says no lookup ran; `engine: 'none'`; widget bundle regenerated.                                                                                                                                                             |
| M/M39–M42             | `1a08d241`             | Count errors surface, incomingAnchors endCursor real, forged list cursors refused (400), companies `degraded` = engine outage only.                                                                                                          |
| B/F5                  | `9ce89556`             | Rankings, commitment rankings, report listings (+ total) and single reports gate on a public-or-absent organization; SQL-pinned.                                                                                                             |
| M/M04                 | `d86a2c53`             | Legal render `s-maxage` 300 s, SWR 60 s.                                                                                                                                                                                                     |
| M/M06                 | `9318c22d`             | `frameworkRole` availability probed from the live build (column + ≥95 % stamped rows, 60 s window); routing, notes, SQL and share validation read it; four Codex rounds.                                                                     |
| M/M10                 | `be3eecab`             | Procurement contributor: bounded, public-only presence counts by role; capped totals ride on the label.                                                                                                                                      |
| N/M2                  | `5eeceae5`             | Public map views memoised (reference cache, 5 min, uploaded datasets bypass, single-flight); public reads on their own 120/min bucket.                                                                                                       |
| I/INS-11              | **closed, no change**  | Owner decision 2026-09-09: INS serves aggregated public statistics only; `privacy_class` on `ins.*` is a uniform loader column with no non-public producer, so gating it adds predicates to hot territory/context reads for no containment.  |

Not changed, by design: N/R3 (no reachable NULL; coercion would hide a contract
violation), N/M3 (module slated for deletion), the B/F3 fail-closed calculation
(kept; only its message was unmasked). Everything in §2.5 and the architectural
items (N/N3 DB pointer, X/F5/F6 consolidation, B/F1 porting, B/F6, X/F2 readiness
semantics, X/F7, M/M04, M/M10/11, INS-01/M09) awaits an owner decision.

## 8. Owner decisions (2026-09-09, second pass)

Recorded verbatim from the decision session; each row supersedes the "Current state"
column of §2.5 and the "awaits an owner decision" list in §7.

| Item                               | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| INS-01, M/M09                      | Retire `api.js`: the kernel build (`redesign-api.js`) is the only server. **Amended after the inventory (same day):** ~20 product modules (notifications, notification-platform/-delivery, email-templates, resend-webhooks, six campaign-* modules, institution-correspondence, learning-progress, user-data, user-events, share, report, agent chat) exist only on `api.js`. Two slices: (1) delete the legacy budget-viz GraphQL modules, legacy INS, legacy MCP + GPT REST, the legacy `/graphql` schema, legacy env keys and the five legacy `src/app/` files now, keeping `api.ts` as a slim composer (kernel + platform modules) so Phoenix prod keeps working, tagging the last full Phoenix commit first; (2) port the platform modules onto `build-redesign-app` (needs Redis/Resend on Chronos), then delete `api.ts`. Agent chat stays off on Chronos dev until slice 2. Confirms the 2026-07-12 decision to delete the legacy `mcp` module and GPT REST (M/M23). N/N1, N/N2, N/M3 and X/F3's base-edit exposure close with slice 1. |
| DP-01 (diagnosis)                  | Root cause and fix location in [`dp-01-creditor-handover.md`](dp-01-creditor-handover.md): the scrapper derives the YTD chain per (entity, creditor, sector); every one of the 170 handovers in 2023–2025 continues the chain, so annual and monthly summaries double count. Fix belongs in the scrapper's flag derivation + MV refresh; server adds a golden assertion. **Owner go-ahead 2026-09-09: implement in the scrapper repo (lead), owner runs the rebuild/refresh on Chronos.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| X/F6 (design)                      | [`xf6-population-consolidation-design.md`](xf6-population-consolidation-design.md): kernel `AnnualPopulationPort`, admission + adapter owned by `ins-native`, `budget/shell/native` consumes it, four commits. **Approved as written 2026-09-09.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| X/F7 (handover)                    | NetworkPolicy for the Chronos gitops repo, namespace `transparenta-eu-dev`, podSelector `app.kubernetes.io/name=hack-for-facts-eb-server`, `policyTypes: [Egress]`, one egress rule `ports: [{port: 443, protocol: TCP}]` to `ipBlock` CIDRs `104.18.37.202/32` and `172.64.150.54/32` (`api.clerk.com` A records on 2026-09-09; the existing `client-cloudflare-api-egress` policy uses the same /32 pattern). Clerk sits behind Cloudflare anycast, so if lookups start failing widen to Cloudflare's published ranges `104.16.0.0/13` and `172.64.0.0/13`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| X/F3 (handover)                    | Phoenix dev Argo app `hack-for-facts-eb-server-dev` (namespace `argocd`, Phoenix cluster) tracks `dev` with `automated: {prune: true, selfHeal: true}` and was synced at `c22d10d4` on 2026-09-09. Pin: set `spec.source.targetRevision` to the tag that slice 1 creates (`phoenix-last-full`, placed on the commit before the deletion lands) and drop `automated` for the migration window.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B/F1, B/F6, B/F2, N/N4, N/N5, B/F4 | Native roots replace the five legacy roots; client migration is the gate. Doc 13 to be rewritten as a deployment matrix. The two additive fields on frozen types are accepted and recorded. B/F6 closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| N/N3                               | Population admission pins stay in code through prod. Document the republish procedure and the nominal-ranking blast radius.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| X/F2                               | `/api/v1/ready` keeps gating on the user database. Accepted and documented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| X/F7                               | Allow egress from the server pods to `api.clerk.com:443` on Chronos (platform gitops, applied by the owner). Server code unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| N/M2                               | Public map reads get a result cache (dataset + params, short TTL) and their own rate bucket separate from writes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| M/M04                              | Legal render responses drop the day-long shared-cache window to private / short `s-maxage`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DP-01                              | Investigate the root cause and fix it correctly so the annual values are right; no caveat-only stopgap. The fix may land in the scrapper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D/DP-02                            | `isUat=true` is strict (UAT only); county councils are reached through `isTerritorialExecutive`. Documented for the client, no server change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D/DP-04                            | Chronos entity-type names are canonical. Stale legacy typedefs updated; old→new mapping recorded for the client migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| M/M10                              | Add the procurement contributor (presence + profile slice by CUI).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| M/M11                              | Amend doc 00: GraphQL + MCP are the primary surfaces; REST per concrete need; per-module OpenAPI requirement dropped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| B/F5                               | Rankings and report listings gate on a public-or-absent organization like grouped analytics.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| M/M39–M42, M/M06                   | Fix all five.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| X/F5                               | ESLint `boundaries` covers `src/app/**`; internal imports routed through module indexes or allowed with a recorded reason.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| X/F6                               | Full consolidation: `AnnualPopulationPort` in `shared/core/ports`, adapter + admissions owned by `ins-native`, `budget` consumes the port, `src/app/` reduced to wiring, unit tests for every moved rule. Design note first, then implementation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| N/N6, N/N7                         | Native adapters required when `budget` is enabled; boot fails without `ins-native`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| §2.7 hygiene                       | All four buckets in this pass: kernel hardening (B/F13–19, N/K1), config & health (X/F13–16), dedupe & dead code (B/F18, N/P3, N/P5), tests & repo (T/T-*, X/F24, import hygiene).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| X/F3                               | Owner pins the Phoenix dev Argo app to a revision; the lead supplies the revision and the change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| §2.6 docs                          | Fix every load-bearing stale row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
