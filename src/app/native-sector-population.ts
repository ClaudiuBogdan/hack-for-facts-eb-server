/** Moved to the INS module (review X/F6); re-exported until the budget adapters move too. */
export {
  readAdmittedSectorPopulation,
  type SectorPopulationAdmission,
} from '../modules/ins-native/index.js';
