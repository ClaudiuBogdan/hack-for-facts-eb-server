/**
 * The one territory the kernel has to name: Bucharest is a county (`county_code
 * 'B'`) whose only pre-L2 geographic row was the municipality (SIRUTA 179132),
 * and whose six sectors are `locality`/`sector` children of that municipality.
 * Named here so no raw SQL carries the literals (review N/K1).
 */
export const BUCHAREST_COUNTY_CODE = 'B';
export const BUCHAREST_MUNICIPALITY_SIRUTA = '179132';
